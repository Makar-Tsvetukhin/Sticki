# Future Work (по ТЗ Roguelike stickman)

## 0) Текущий фокус: Белый враг (MeleeEnemy)

### Уже сделано
- [x] Визуал белого врага (материал/свечение) настроен.
- [x] Хитбоксы `HB_*` созданы и расставлены.
- [x] Слой `EnemyHitbox` используется для хитбоксов.
- [x] `EnemyHitboxProxy` добавлен на все `HB_*`.
- [x] `EnemyHealth` добавлен на корень префаба `MeleeEnemy`.
- [x] Базовая цепочка урона работает: `Raycast -> EnemyHitboxProxy -> EnemyHealth`.
- [x] Коллайдер катаны добавлен.
- [x] Базовые оптимизации рендера врага (тени/пробы/motion vectors) выполнены.
- [x] NavMesh-преследование и базовая melee-атака работают.
- [x] Цикл анимаций ближнего боя стабилизирован для MVP.
- [x] Смерть врага через белый VFX подключена.
- [x] Смерть врага переведена на пул VFX (без `Instantiate/Destroy` на каждый килл).
- [x] `Write Defaults` в контроллере врага переведён в `Off`.

### Доделать по текущему этапу
- [x] Урон игроку от катаны (`KatanaDamage`) и базовые правила хита.
- [x] Базовая проверка боя врага в сцене (hit/death/chase/attack).
- [ ] Полировка таймингов атаки/дистанций/поворота (баланс, не багфикс).
- [ ] Исправить коллизии игрок-враг: игрок не должен проходить сквозь врагов и "бегать по ним".
- [ ] Исправить баг входа в бой: при стрельбе в упор по пассивному врагу игрок может получить урон от катаны при переходе врага в combat (без корректной фазы удара).
- [ ] Привести урон катаны к нормальной системе окна удара: урон только в валидной фазе/кадрах анимации атаки.
- [ ] Полный прогон на масс-спавне (20/50/100+) с профайлером.
- [x] Финальный VFX смерти врагов готов и не требует переделки.



### Статус этапа
- [x] Этап белого врага закрыт как MVP.
- [x] Финальная полировка белого врага перенесена в общий этап полировки.

## 1) MVP-контур (что нужно для рабочей первой версии)

- [x] Первая арена проходима на уровне MVP (вход/выход/спавн/завершение).
- [ ] Белый враг боеспособен и стабилен в масс-бою.
- [x] Базовая стрельба, перезарядка и базовое переключение оружия собраны как каркас.
- [x] Минимальный UI (HUD + меню + пауза + базовые настройки).
- [x] Безопасная зона и базовый выбор улучшений между аренами.
- [x] Локальный результат сессии (комнаты/киллы/время + таблица рекордов).

## 2) Арены (Level Design)

### Арена 1 — Начальная
- [ ] Финальная геометрия по ТЗ.
- [x] Валидные точки старта игрока и выхода на уровне текущего MVP.
- [x] Логика двери: закрыта до зачистки, открывается после.
- [ ] SFX: звук закрытия замка/двери при входе игрока на арену (NoEntry).
- [x] Начальные враги и динамический спавн подключены (ручной `EnemyParent` + авто-точки по NavMesh).
- [ ] Проверка читабельности укрытий и перемещения.

### Арена 2 — Треугольная
- [x] Базовый свет для арены выставлен.
- [x] Общая геометрия арены собрана.
- [ ] Геометрия вершин и центра по ТЗ.
- [ ] Реализация геймплейной идеи «удержание/смена вершины».
- [ ] Спавн-логика с акцентом на вершины.

### Арена 3 — Кольцевая двухуровневая
- [x] Базовая геометрия арены собрана.
- [x] Пробы/скриптовые точки на арене расставлены.
- [ ] NavMesh для арены ещё не собран.
- [ ] Два полноценных уровня + винтовая лестница.
- [ ] Спавн/давление по этажам.
- [ ] Проверка тактики «низ осторожно / верх агрессивно».

### Арена 4 — Звезда
- [x] Базовая геометрия звезды и центральной платформы собрана как контент.
- [ ] Давление из лучей, баланс центральной позиции.
- [ ] Проверка угрозы от дальнего врага в центре.

## 3) Враги

### Белый (ближний бой)
- [ ] Полный цикл поведения: passive -> combat -> chase -> attack -> recover.
- [ ] Работа с вертикальностью (прыжки/обход).
- [ ] Групповое давление и окружение (без «паровозика»).

### Синий (дальний бой)
- [x] Модель и анимационный контент (FBX + базовые клипы + controller) добавлены в проект.
- [x] Базовая дистанционная атака собрана на игровом префабе `Range`.
- [x] Телеграф выстрела (`LineRenderer`) переведён на animation events `PrepStarted/PrepFinished`.
- [x] Нанесение урона переведено на animation event `FireShot`.
- [x] Завершение цикла стрельбы переведено на animation event `FireFinished`.
- [x] Направление телеграфа и выстрела отвязано от локальной оси `firePoint` и считается по aim point.
- [x] Попадание по игроку через дочерние коллайдеры исправлено (`GetComponentInParent<PlayerHealth>`).
- [ ] Дистанционная атака с фазой подготовки — финальная балансировка таймингов/длительности клипов.
- [ ] Выбор позиции/линия видимости/отход под угрозой.
- [ ] Взаимодействие с ближними врагами (стрельба из-за них/из-за укрытий).
- [ ] Разделить поворот root и прицеливание: корпус/оружие должны сопровождать игрока по горизонтали и вертикали, без разворота всего врага как статуи.
- [ ] Добавить vertical aim/pitch для дальнего врага: если игрок выше или ниже, враг должен поднимать/опускать корпус или оружие, а не смотреть только вперёд по горизонту.
- [ ] Проверить в Unity, что все 4 animation events стоят на правильных клипах и не потеряются при переимпорте/замене анимаций.
- [x] Проверить `Range.prefab`: ссылки на `Animator`, `firePoint`, `LineRenderer` и event receiver после всех правок.

## 4) ИИ (по ТЗ)

### Общая логика боя
- [x] Глобальная активация боя на арене после первого боевого действия (базовый слой через `GlobalCombatActive`).
- [x] Знание позиции игрока после активации (MVP: враги резолвят `PlayerHealth.Instance`).
- [ ] Координация врагов (фланги/перекрытие отхода).

### ИИ ближнего боя
- [x] Не отступает, не отменяет атаку, не теряет игрока (MVP-поведение белого врага).
- [x] Преследование с переоценкой маршрута (базовый `ResolveChaseDestination`/`NavMeshPath`).
- [ ] Ограничение попыток в недостижимую геометрию.

### ИИ дальнего боя
- [ ] Не стреляет без LOS, меняет позицию при угрозе.
- [ ] Держит дистанцию и использует укрытия/союзников.
- [ ] Избегает тупиковых/невыгодных позиций.
- [ ] Привести `CheckLOS()` у дальнего врага к реальной точке выстрела (`firePoint`), а не к позиции корпуса.
- [ ] Проверить поведение на укрытиях: AI не должен считать, что LOS есть, если реальный выстрел упирается в стену/свой меш.
- [ ] Подумать над схемой aim rig / отдельного узла для upper body, чтобы yaw/pitch шли в корпус и оружие, а навигационный root не ломал анимацию.

## 4.1) ИИ до финального (отдельный трек)

### Белый враг: доведение до production-качества
- [ ] Устойчивые переходы состояний без частных костылей.
- [ ] Внятные правила re-path/re-target при динамичном бою.
- [ ] Нормальная работа в толпе (без постоянного слипания/дёрганий).
- [ ] Поведение при потере игрока, смене уровня высоты, тупиках NavMesh.

### Синий враг: полноценный дальний ИИ
- [ ] Базовый state machine: seek position -> aim -> fire -> reposition.
- [ ] Выбор точки с учётом LOS, укрытий и дистанции.
- [ ] Реакции на угрозу вблизи (kite/отход/смена позиции).
- [ ] Полный smoke-test синего врага: `Prep -> Fire -> hit -> FireFinished -> следующий цикл` без зависаний.
- [ ] Проверить массовый бой с несколькими ranged enemy одновременно: нет ли зависаний в `isShooting`, пропавшего телеграфа или рассинхрона event'ов.

### Групповое поведение
- [ ] Ближние и дальние враги не мешают друг другу в критичных точках.
- [ ] Давление на игрока читаемое, но без нечестных пиков.
- [ ] Базовая система ролей/приоритетов в волне.

## 5) Система спавна врагов

### Уже сделано
- [x] Базовый `ArenaSpawnConfig` (волны, лимиты, типы врагов, тайминги).
- [x] `ArenaSpawner` с запуском боевой фазы после первого боевого события.
- [x] Пуллинг врагов (prewarm, reuse, возврат в пул по смерти).
- [x] Учёт стартовых врагов, поставленных вручную в `EnemyParent`.
- [x] Автогенерация точек спавна по NavMesh (без ручной расстановки сотен пустых точек).
- [x] Выбор точек с учётом дистанции, LOS и позиции относительно игрока.
- [x] Ограничение одновременно живых (`hardAliveCap`).
- [x] Ранний старт следующей волны по `earlyNextWaveKillRatio`.
- [x] Событие зачистки арены (`OnArenaCleared`) при выполнении условий.

### Доделать
- [ ] Пропорции типов врагов по волнам (пока фактически только белый враг).
- [ ] Явная защита от появления в кадре игрока (жёсткий anti-on-screen rule).
- [ ] Fallback-поведение при нехватке валидных точек (доработать стратегию retries/degrade).
- [x] Привязать завершение арены к двери/переходу на следующий этап.
- [x] Вынести runtime-метрики спавна в debug/HUD (alive/spawned/killed по арене).

## 6) Прогрессия сложности

- [x] Усиление через объём врагов (базовый слой через `ArenaDifficultyConfig`: dynamic spawn / wave size / alive cap).
- [x] Усиление через временное давление (базовый слой через tick interval / inter-wave delay).
- [x] Усиление через боевую опасность врагов (базовый слой: HP и урон врагов).
- [x] Масштабирование сложности по номеру арены/комнаты.

## 7) Главный герой

### Передвижение
- [ ] Финальный movement feel (ходьба/бег/ускорения/контроль в воздухе по ТЗ).
- [ ] Подобрать и настроить корректный коллайдер игрока (замена текущей капсулы при необходимости).
- [ ] Защита от просмотра сквозь стены/клиппинга головой.

### Бой и выживание
- [x] Базовая стрельба/перезарядка/смена оружия — каркас.
- [x] Система получения урона игроком (melee/ranged источники на уровне MVP).
- [x] Смерть игрока + базовое завершение сессии.

### Оружие
- [x] Базовый пистолет.
- [x] Базовый AR — каркас.
- [x] Пулемёт — базовый weapon/view model контент и переключение подключены.
- [x] Дробовик (базовый: поштучная перезарядка, прерывание выстрелом, VM-анимации `start/insert/end`, shell visual).

## 8) Элементы взаимодействия

- [x] Взаимодействие с врагами/лутом на уровне MVP.
- [x] 3D-модели патронов и здоровья добавлены в проект как контент.
- [x] Подбор патронов.
- [x] Восстановление здоровья.
- [x] Механика дропа с врагов: шанс выпадения патронов и аптечек по таблице вероятностей.
- [x] Автоподбор дропов наступанием (без отдельной кнопки интеракта), с корректным лимитом по HP/боезапасу.
- [x] Переход на следующую арену.
- [x] Безопасная зона и выбор улучшений.
- [x] Определить UX выбора улучшений: наведение + кнопка, выстрелом или отдельным интерактом.
- [x] Стена выбора оружия.
- [x] Стена модификаторов оружия.
- [x] Стена модификаторов персонажа.
- [x] Явная визуальная обратная связь при выборе.

## 9) Система улучшений

### Текущий статус
- [x] База системы улучшений собрана на уровне MVP foundation.
- [x] `UpgradeSelectionController` больше не является источником данных для character/weapon upgrades; он оставлен как UI/world-interaction слой.
- [x] Данные улучшений вынесены в `UpgradeDefinition` + `UpgradeCatalog`.
- [x] Состояние выбранных улучшений сессии вынесено в `RunUpgradesState`.
- [x] Генерация вариантов вынесена в runtime-слой (`UpgradeRollGenerator`) и использует `seed`, `maxStacks`, веса и недопущение дублей в одном ролле.
- [x] Применение character upgrades и weapon upgrades разведено по отдельным applier-слоям.
- [x] Пересчёт идёт централизованно через `UpgradeRuntimeController`.
- [x] Для оружия введён runtime-слой (`WeaponRuntimeStats`), чтобы weapon upgrades не смешивались с UI и не хранились как текст.
- [x] Переход `Ar1 -> UpgradeRoom` и базовая синхронизация сцен с upgrade runtime собраны и проверены в Play Mode как рабочий MVP flow.

### Что считать завершённым на текущем этапе
- [x] Data-driven архитектура улучшений для персонажа и оружия.
- [x] Базовый пул и выдача вариантов между аренами.
- [x] Базовое применение улучшений к персонажу и оружию.
- [x] Стеки и ограничения по повторному выбору.
- [x] Сессионное состояние улучшений без постоянного сохранения.

### Что осознанно НЕ считается завершённым
- [ ] Полный контент-пул улучшений из ТЗ.
- [ ] Финальные редкости/баланс/веса выпадения по всем улучшениям.
- [ ] Иконки и финальная презентация карточек.
- [ ] Полная локализация названий/описаний.
- [ ] Сложные событийные эффекты (`OnHit`, `OnKill`, `proc`, статусные взаимодействия).
- [ ] Полный weapon progression flow и финальный выбор/смена оружия через ту же систему.

### Контент улучшений
- [ ] Улучшения оружия: калибр, крит, разрыв, пробитие, зажигательные и т.д.
- [ ] Модификаторы персонажа: скорость, живучесть, дропы, экономика и т.д.

### Алгоритмы и архитектура
- [x] Формирование пула улучшений.
- [x] Алгоритм выдачи выбора.
- [x] Применение улучшения к состоянию игрока/оружия.
- [x] Хранение состояния улучшений в сессии.
- [ ] Обработка событий и ограничений (стеки/уникальность/совместимость) — базовый слой готов, сложные совместимости и событийные эффекты остаются на следующий этап.

## 10) Логика игровых систем

### Камера и ViewModel
- [ ] Поведение камеры в бою (кач, отдача, ограничения).
- [ ] Стабильные VM-анимации и переходы.
- [ ] ViewModel-эффекты оружия (вспышка выстрела/muzzle flash, базовые VFX выстрела).
- [x] Решение клиппинга оружия в геометрию.
- [ ] Hit feedback: индикатор попадания, визуальный фидбек по урону врагу и игроку.
- [ ] Синхронизировать AR: скорострельность и анимацию выстрела (привести к одному таймингу).

### Обучение
- [x] Система обучения и инфо-окна — базовая страница `Как играть` добавлена.
- [x] Подача правил без перегруза игрока — базовый how-to экран добавлен.

### Стрельба и урон
- [ ] Полный порядок обработки попадания.
- [ ] Критический урон.
- [ ] Статус-эффекты.
- [ ] Пробитие/разрывные механики.
- [ ] Точность/отдача/задержки/боезапас.

### Получение урона игроком
- [x] Базовые правила применения урона и ограничения (`PlayerHealth`, i-frames после хита).
- [ ] Разделение melee/ranged урона.
- [x] Базовая реакция на урон/смерть игрока.
- [ ] Особая логика улучшения «Читер?» (отмена смерти).

## 11) Навигация и перемещение (архитектура)

- [x] Игрок: компонентная схема перемещения (`PlayerMotor`/`PlayerLook`/`PlayerInputSource`/`PlayerControlCoordinator`).
- [ ] Враги: единый навигационный слой.
- [ ] Модель ответственности ИИ и навигации.
- [ ] Требования к NavMesh и уровням.
- [ ] Оптимизация частоты обновления маршрутов.
- [ ] Обработка застреваний.
- [ ] Варианты анти-застревания:
- [ ] Локальный repath при отсутствии прогресса.
- [ ] Мягкий `nudge` (малый сдвиг из пересечения).
- [ ] Fallback телепорт на ближайшую валидную точку NavMesh при критическом застревании.
- [ ] Fail-safe для спавна/застревания: если враг оказался внутри геометрии или долго без прогресса, делать `NavMesh.SamplePosition` + `agent.Warp` на ближайшую валидную точку.
- [ ] Избегание столкновений и групповое движение.
- [ ] Интеграция навигации с боем и спавном.

## 12) UI/UX

- [x] Главное меню (базовый экран + переходы на страницы).
- [x] Пауза (базовый экран + Resume/Main Menu/Quit).
- [x] Пауза при потере фокуса окна (`Alt+Tab`) в бою.
- [x] Настройки (базовый экран + сохранение через `GameSettingsService`):
- [x] Звук (master/SFX/music sliders; master volume применяется глобально).
- [x] Чувствительность мыши.
- [x] FOV.
- [ ] Лимит FPS / VSync.
- [ ] Разрешение и режим окна (fullscreen/windowed/borderless).
- [ ] Язык.
- [ ] Переназначение клавиш (keybinds) и сохранение биндов.
- [x] HUD (HP, патроны, текущее оружие, прицел) — базовый каркас.
- [x] Добавить в HUD счётчик зачистки арены: `kills/target` (например, `0/200`), с достижением `target/target` как явным фидбеком завершения.
- [x] Улучшить читаемость прицела на светлых целях/фоне: сменить цвет (например, зелёный) и проверить контраст в бою.
- [x] Добавить плавную анимацию убывания HP-бара и обновления числового HP (без резких скачков).
- [x] Подсказка взаимодействия возле прицела (справа-снизу от него): формат `КНОПКА — ДЕЙСТВИЕ`, с учётом ребиндов.
- [x] Система подсказок для всех интерактивных объектов (двери, оружие, улучшения).
- [ ] Локализация всех UI-текстов (HUD, меню, подсказки, описания улучшений) с учётом ребиндов клавиш.
- [x] Экран смерти и результат сессии — базовый каркас.
- [x] Локальная таблица рекордов (MVP: `RunRecordsStorage` + `RecordsController`).

### Архитектура UI (общая система)

#### Уже сделано
- [x] Введены общие root-префабы для UI:
- [x] `FrontendUIRoot` для фронтенда/главного меню.
- [x] `GameplayUIRoot` для игрового UI.
- [x] Добавлены общие root-контейнеры `FrontendRoot.uxml` и `GameplayRoot.uxml`.
- [x] Добавлен общий `AppTheme.uss` для root-слоя и wrapper-экранов.
- [x] Добавлен общий базовый слой UI-контроллеров:
- [x] `UIScreenController`.
- [x] `UIRootController`.
- [x] `FrontendUIRootController`.
- [x] `GameplayUIRootController`.
- [x] Добавлены специализированные экранные контроллеры под root-схему:
- [x] `MainMenuUIController`.
- [x] `SimpleScreenController`.
- [x] `UpgradeSelectionUI`.
- [x] Главное меню, настройки, рекорды, how-to и библиотека улучшений собраны под `FrontendUIRoot`.
- [x] HUD, пауза, экран смерти, `RunTab` и UI выбора улучшений собраны под `GameplayUIRoot`.
- [x] Сцены `MainMenu`, `Ar1` и `UpgradeRoom` переведены на root-prefab подход вместо россыпи отдельных `UIDocument`-объектов.
- [x] Логика world interaction для комнаты улучшений частично отделена от чистого UI-слоя (`UpgradeSelectionController` + `UpgradeSelectionUI`).
- [x] `PanelSettings` подключён к новым root-prefab.
- [x] `HudRuntimeBinder` подключён обратно в `GameplayUIRoot`.
- [x] Старый `MainMenuController` убран из `MainMenu` как основной способ навигации; сцена использует `FrontendUIRoot`.

#### Доделать
- [ ] Убрать двойную систему show/hide:
- [ ] Должен остаться один источник истины для видимости экранов.
- [ ] Убрать конфликт между внешними wrapper-экранами root-системы и старыми внутренними классами вроде `pause-root--visible` / `upgrade-selection-root--visible`.
- [ ] Довести до конца root-архитектуру всех экранов, чтобы каждый экран был полноэкранным внутри общего `UIDocument` и не зависел от старой структуры корня.
- [ ] Дочистить оставшиеся старые scene-level контроллеры/мёртвые объекты после миграции, если они ещё где-то не участвуют в новой схеме.
- [ ] Проверить и вычистить все `FindFirstObjectByType` в UI-контроллерах, где они остались как временный способ связывания.
- [ ] Почистить битую кириллицу/кодировку в UXML, USS и C# UI-скриптах.
- [ ] Проверить порядок слоёв, `pointer-events`, `display`, `position` и `sorting`, чтобы HUD не блокировал мир, а оверлеи корректно перекрывали игру.
- [ ] Вынести правила переходов между экранами в устойчивую схему (front/menu/game overlays), без частных исключений в каждом контроллере.
- [ ] Довести `UpgradeSelection` до нормального разделения ответственности:
- [ ] world interaction / raycast / shield highlight отдельно.
- [ ] UI view отдельно.
- [ ] применение апгрейдов и состояние сессии отдельно.
- [ ] Сделать регресс-прогон UI после каждого изменения root-системы:
- [ ] MainMenu.
- [ ] Pause.
- [ ] Death screen.
- [ ] RunTab.
- [ ] UpgradeSelection.
- [ ] HUD updates.

### Уже сделано (базовый UI-каркас меню)
- [x] Отдельный экран `Records` в стиле проекта.
- [x] Отдельный экран `Библиотека улучшений` (locked/unlocked карточки, `???` и чёрный силуэт для скрытых).
- [x] Отдельный экран `Как играть`.
- [x] Базовая навигация между экранами (`MainMenu <-> Settings <-> Records <-> Upgrades <-> HowTo`).
- [x] Возврат в меню по `Esc` и по кнопке `Назад` на страницах.
- [x] Кастомизированные скроллбары на новых страницах под общий визуальный стиль.
- [x] Базовый `Tab`-оверлей в бою (текущая комната/арена, время забега, список текущих улучшений).
- [x] Базовый экран смерти (`Вы погибли`) с кнопками `Рестарт` и `В главное меню`.
- [x] Рестарт забега через перезагрузку текущей сцены (`SceneManager.LoadScene(activeScene)`).
- [x] Базовая интеграция состояния смерти: управление игроком отключается, UI смерти показывается.

### Доделать по меню-UI (следующая итерация)
- [x] Подключить реальные данные рекордов вместо временного placeholder (сейчас хранится top-50, UI выводит сохранённые записи).
- [ ] Подключить реальные данные открытия улучшений в библиотеке, вместо временного счётчика/демо-контента.
- [x] Привязать кнопку `Настройки` в паузе к in-game экрану настроек.
- [ ] Финальная локализация текстов новых страниц (сейчас рабочая версия на русском).
- [ ] Довести `Tab`-оверлей улучшений до production-качества (финальная компоновка/иконки/данные из системы улучшений без ручного списка).
- [ ] Довести экран смерти до финала (полные метрики забега + интеграция в систему рекордов).
- [x] Включить `autoPauseOnFocusLost` в базовой конфигурации паузы.
- [ ] После стабилизации root-системы проверить все экраны на единый UX показа/скрытия, клавиши `Esc`, курсор и блокировку инпута.

## 12.1) Переходы и загрузки

- [ ] Спроектировать и реализовать переходы между аренами без заметных фризов.
- [ ] Продумать загрузочный экран/фейд.
- [ ] Проверить сохранение состояния сессии при переходе.
- [ ] Сохранение позиции/фазы "солнца" в главном меню, чтобы при повторном входе время суток продолжалось, а не стартовало заново.

## 13) Локализация

- [x] Русский интерфейс на уровне текущего MVP.
- [ ] Английский интерфейс.
- [ ] Переключение языка в настройках.

## 14) Аудио

- [ ] Звуки выстрелов/перезарядки/попаданий.
- [ ] Звуки врагов (атака/получение урона/смерть).
- [ ] UI-звуки.
- [ ] Фоновая музыка и микс по фазам боя/безопасной зоны.

## 14.2) UX/SFX/VFX полировка (обязательный этап перед финалом)

### UX и обратная связь
- [ ] Подтвердить финальный UX выбора улучшений (интеракт/наведение/выстрелом).
- [ ] Hit feedback по врагу и игроку (визуал + звук + UI-индикация).
- [ ] Ясный фидбек входа/выхода из боевой фазы.
- [ ] Читаемый фидбек критов/особых эффектов/смерти врага.

### Звук
- [ ] Полный baseline SFX: шаги, выстрелы, попадания, смерть, UI, двери, улучшения.
- [ ] Аудио-микс по приоритетам (не забивать важные сигналы в бою).
- [ ] Проверка читаемости звука на низкой/средней/высокой громкости.

### VFX
- [ ] Финальные интенсивности и длительности VFX для читаемости в толпе.
- [ ] Ограничение одновременных эффектов + деградация качества при нагрузке.
- [ ] Сверка VFX с реальным TTK/темпом боя, чтобы не мешали геймплею.

## 14.3) Рефакторинг под конфиги и расширяемость

### Конфиги и данные
- [ ] Вынести ключевые параметры врагов в `EnemyConfig` (`ScriptableObject`).
- [x] Вынести базовые параметры оружия в `WeaponConfig`.
- [x] Добавить `ArenaConfig` для волн и лимитов (базовый `ArenaSpawnConfig` уже в работе).
- [ ] Добавить слой runtime-модификаторов сложности поверх `ArenaSpawnConfig` (директор сложности).
- [ ] Развести код логики и контентные данные (без хардкода чисел в скриптах).

### Системная расширяемость
- [ ] Единый контракт для урона/смерти/эффектов (`IDamageable` + events).
- [ ] Единый пайплайн спавна через пулы (частично сделано: враги + VFX + лут; осталось снаряды/прочее).
- [ ] Простое подключение нового врага/оружия через конфиг и префаб.
- [ ] Инструмент быстрых баланс-итераций без правок кода.

### Баланс-пайплайн
- [ ] Фиксация метрик: DPS, TTK, плотность врагов, время комнаты.
- [ ] Чек-лист плейтеста и таблица изменений баланса по версиям.
- [ ] Быстрый цикл: настройка конфигов -> прогон сцены -> корректировка.

## 14.1) VFX (полный базовый набор)

### Оружие игрока
- [ ] Muzzle flash для каждого оружия (пистолет/AR/пулемет/дробовик).
- [ ] След пули / tracer (где уместно).
- [ ] Impact VFX по разным поверхностям (стена/металл/дерево/враг).
- [ ] Небольшой smoke/heat после серии выстрелов (для автоматического оружия).
- [ ] VFX перезарядки (минимум: акцент ключевого кадра, если нужно).

### Попадания и урон
- [ ] Hit VFX по врагу (легкий всплеск/искра/частицы).
- [ ] Crit VFX (отдельный акцент при крите).
- [ ] VFX статус-эффектов (огонь/взрыв/прочие модификаторы).
- [ ] Feedback по урону игрока (vignette/flash/screen FX).

### Враг ближнего боя (Белый)
- [ ] VFX получения урона.
- [x] VFX смерти: финальный эффект подключён и принят.

### Враг дальнего боя (Синий, на этапе внедрения)
- [ ] VFX выстрела и снаряда врага.
- [ ] VFX попадания по игроку/окружению.
- [x] VFX смерти: финальный эффект подключён и принят.

### Система и производительность VFX
- [ ] Пуллинг всех часто используемых VFX (muzzle/impact/death fx).
- [ ] Лимиты одновременных эффектов (cap), чтобы не просаживать FPS.
- [ ] Настройки качества VFX (Low/Medium/High) в меню.
- [ ] Проверка читаемости VFX в бою и на разных аренах.

## 15) Технический долг и QA

- [ ] Убрать лишние `Debug.Log` перед тестами производительности.
- [ ] Чистая Console (без постоянного warning/error спама).
- [ ] Тест производительности на масс-волнах.
- [ ] Проверка слоёв/коллизий по всей игре.
- [ ] Критичный баг коллизий: игрок может забираться/бежать по врагам и пересекать их некорректно.
- [ ] Проверить edge-case: урон игроку не должен проходить от катаны вне окна атаки (включая момент агра/входа врага в combat).
- [ ] Добавить обработку выхода за пределы арены (kill zone/телепорт/штраф по ТЗ).
- [ ] Регресс-тест после каждой крупной правки систем.
- [ ] Проверить после последних оптимизаций, что `PerformanceOverlay` больше не делает тяжёлых обходов сцены и не даёт CPU spikes.
- [ ] Проверить, что замена `RaycastAll` на `RaycastNonAlloc` в `PlayerCombat` не ломает регистрацию ближайшего валидного хита.
- [ ] Проверить, что HUD корректно показывает символ `∞` и не ломает шрифт/кодировку в финальной сборке.
- [ ] Пуллинг для массовых объектов:
- [x] Враги.
- [ ] Снаряды/трейлы/следы попаданий.
- [x] VFX (минимум death VFX врага).
- [x] Лут/дропы (`Ammo`/`HP`).
- [ ] Минимальные smoke-тесты (далее, ближе к полировке):
- [ ] Старт сцены -> бой -> смерть врага.
- [ ] Переход арены -> сохранение прогресса.
- [x] Смерть игрока -> экран результата/перезапуск.

## 16) Порядок выполнения (рекомендуемый)

1. Закрыть текущий этап белого врага (урон катаны, i-frames, смерть).
2. Довести арену 1 + спавнер волн + дверь выхода.
3. Довести минимальный UI и завершение сессии.
4. Подключить безопасную зону и базовый выбор улучшений.
5. Ввести синего врага и базовую комбинированную волну.
6. Перейти к системному рефакторингу (`EnemyConfig`, улучшения, навигация, масштабирование).

## 17) Что уже готово как контент/ассеты

- [x] Сцены `MainMenu`, `Ar1`, `Ar2`, `Ar3`, `Ar4`, `UpgradeRoom` существуют в проекте.
- [x] Геометрический контент всех 4 арен добавлен в проект.
- [x] Для `Ar1` и `Ar2` есть NavMesh-ассеты в проекте.
- [x] Белый враг присутствует как игровой префаб и как анимационный контент.
- [x] Синий враг присутствует как модель и анимационный контент, но ещё не доведён до игрового AI/prefab flow.
- [x] View model для `Pistol`, `AR`, `LMG`, `Shotgun` присутствуют в проекте.
- [x] Конфиги оружия для `Pistol`, `AR`, `LMG`, `Shotgun` присутствуют в проекте.
- [x] 3D-модели дропов `Ammo` и `HP` присутствуют в проекте.
